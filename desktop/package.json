{
  "name": "ai-factory-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell for the AI-FACTORY control center.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --mac --win --linux",
    "native:build": "cd macos/AIFactoryNative && swift build",
    "native:run": "cd macos/AIFactoryNative && swift run"
  },
  "devDependencies": {
    "electron": "^37.0.0",
    "electron-builder": "^24.0.0"
  }
}

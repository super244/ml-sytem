instance:
  type: evaluate
  name: example-evaluate
  environment:
    kind: local
orchestration_mode: single
experience:
  level: dev
lifecycle: {}
execution:
  backend: local
subsystem:
  command_override:
    - python3
    - "-c"
    - "print('Replace subsystem.command_override with your evaluation driver.')"

/**
 * OpenAPI-derived types (`npm run api:sync`). Use `paths` / `components` for HTTP shapes;
 * domain-specific helpers remain in `../api.ts`.
 */
export type { components, operations, paths } from './generated/schema';
